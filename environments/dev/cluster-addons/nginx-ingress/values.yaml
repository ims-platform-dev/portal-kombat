# Public nginx-ingress (for Port.io webhooks, external services)
public:
  controller:
    # IngressClass
    ingressClassResource:
      name: nginx
      enabled: true
      default: true
      controllerValue: "k8s.io/ingress-nginx"

    service:
      type: LoadBalancer
      annotations:
        service.beta.kubernetes.io/aws-load-balancer-type: "external"
        service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
        service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: "ip"
        service.beta.kubernetes.io/aws-load-balancer-backend-protocol: "tcp"
        service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
        external-dns.alpha.kubernetes.io/hostname: controller.plt-dev.inteleradhealth.com

    # Metrics
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
        namespace: ingress-nginx

    # Rate limiting (critical for public endpoints)
    config:
      limit-req-status-code: "429"
      limit-conn-status-code: "429"

      # Global rate limits
      limit-rps: "100"
      limit-connections: "100"

      # Request size limits
      proxy-body-size: "10m"
      client-body-buffer-size: "1m"

      # SSL/TLS Configuration (cert-manager provides certificates)
      ssl-protocols: "TLSv1.2 TLSv1.3"
      ssl-ciphers: "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384"
      ssl-prefer-server-ciphers: "true"

      # Security headers
      hide-headers: "Server,X-Powered-By"
      hsts: "true"
      hsts-max-age: "31536000"
      hsts-include-subdomains: "true"
      hsts-preload: "true"

      # Force HTTPS redirect
      force-ssl-redirect: "true"
      ssl-redirect: "true"

      # Logging
      log-format-upstream: '$remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent" $request_length $request_time [$proxy_upstream_name] [$proxy_alternative_upstream_name] $upstream_addr $upstream_response_length $upstream_response_time $upstream_status $req_id'

    # Resources
    resources:
      requests:
        cpu: 100m
        memory: 90Mi
      limits:
        cpu: 500m
        memory: 256Mi

    # Replicas
    replicaCount: 2

    # Node affinity (prefer spot for cost savings)
    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                - ingress-nginx
            topologyKey: kubernetes.io/hostname

# Internal nginx-ingress (for Grafana, ArgoCD, Prometheus)
internal:
  controller:
    # IngressClass
    ingressClassResource:
      name: nginx-internal
      enabled: true
      default: false
      controllerValue: "k8s.io/ingress-nginx-internal"

    # Service configuration
    service:
      type: LoadBalancer
      annotations:
        # NLB configuration - TCP passthrough for cert-manager TLS termination
        service.beta.kubernetes.io/aws-load-balancer-type: "external"
        service.beta.kubernetes.io/aws-load-balancer-scheme: "internal"
        service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: "ip"
        service.beta.kubernetes.io/aws-load-balancer-backend-protocol: "tcp"
        # Cross-zone load balancing for high availability
        service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
        # External DNS - automatic DNS record creation
        external-dns.alpha.kubernetes.io/hostname: controller.plt-dev.inteleradhealth.com

    # Metrics
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
        namespace: ingress-nginx

    # Config (more relaxed for internal)
    config:
      # SSL/TLS Configuration (cert-manager provides certificates)
      ssl-protocols: "TLSv1.2 TLSv1.3"
      ssl-ciphers: "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384"

      # Force HTTPS redirect
      force-ssl-redirect: "true"
      ssl-redirect: "true"

      # Logging
      log-format-upstream: '$remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent"'

    # Resources
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 128Mi

    # Replicas
    replicaCount: 2

    # Node affinity
    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                - ingress-nginx
            topologyKey: kubernetes.io/hostname
