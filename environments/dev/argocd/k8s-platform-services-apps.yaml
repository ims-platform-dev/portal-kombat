---
# Platform Services App-of-Apps
# Deploys: cert-manager → external-dns → nginx-ingress
# Each service has sync-wave ordering to ensure proper dependency handling
# Note: EBS CSI driver and cluster-autoscaler are deployed via Terraform as EKS add-ons

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-k8s-cert-manager
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10" 
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:ims-platform-dev/portal-kombat.git
    targetRevision: main
    path: environments/dev/cluster-addons/cert-manager
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-k8s-external-dns
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20"  # After cert-manager
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:ims-platform-dev/portal-kombat.git
    targetRevision: main
    path: environments/dev/cluster-addons/external-dns
    helm:
      releaseName: dev-k8s-external-dns
      valueFiles:
        - values.yaml
      skipCrds: false
      parameters: []
  destination:
    server: https://kubernetes.default.svc
    namespace: external-dns
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-k8s-nginx-ingress
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "30"  # After external-dns
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:ims-platform-dev/portal-kombat.git
    targetRevision: main
    path: environments/dev/cluster-addons/nginx-ingress
    helm:
      releaseName: dev-k8s-nginx-ingress
      valueFiles:
        - values.yaml
      skipCrds: false
      parameters: []
  destination:
    server: https://kubernetes.default.svc
    namespace: ingress-nginx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

