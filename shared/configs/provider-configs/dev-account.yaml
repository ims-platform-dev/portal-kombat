---
# ProviderConfig for AWS Dev Account
# This configures how Crossplane authenticates to AWS
apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  # Use IRSA (IAM Roles for Service Accounts) for authentication
  credentials:
    source: IRSA
